name: workflow
on:
  push:
    branches:
      - main
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: print
        uses: actions/checkout@v4
      - name: read
        run: cat index.html
      
