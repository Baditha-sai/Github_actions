name: env
on:
  workflow_dispatch
env:
  app_env: "dev"
jobs:
  env-demo:
    env:
      app_env: test
    runs-on: ubuntu-latest
    steps:
      - name: env
        env:
          app_env: prod
        run: echo "app is connecting to $app_env"
